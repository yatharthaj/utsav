{{-- @if(!empty($partners)) --}}
    {{-- medias start --}}
 {{--    <div class="container-fluid">
        <h4 class="center-align">Associated with:</h4>
        <div class="row owl-carousel owl-theme partners">
            @foreach($partners as $partner)
                <img src="{{$partner->path}}"  alt="{{$partner->name}}" class="responsive-img item">
            @endforeach
        </div>
    </div> --}}
    {{-- medias end --}}
{{-- @endif --}}
